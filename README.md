# Patrocinio de San José — Landing (Next.js)
Proyecto listo para correr en Vercel.

## Uso
npm i
npm run dev
# o
npm run build && npm start

Edita la config del evento en `pages/index.tsx` (objeto `window.CONFIG`).
